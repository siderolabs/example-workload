# commit to be tagged for the new release
commit = "HEAD"

project_name = "example-workload"
github_repo = "siderolabs/example-workload"
match_deps = "^github.com/(siderolabs/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
