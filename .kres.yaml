---
kind: golang.Build
spec:
  outputs:
    linux-amd64:
      GOOS: linux
      GOARCH: amd64
    linux-arm64:
      GOOS: linux
      GOARCH: arm64
---
kind: common.Image
name: image-example-workload
spec:
  extraEnvironment:
    PLATFORM: linux/amd64,linux/arm64
